import { StyleSheet } from 'react-native';

import { hp, wp } from 'src/utils';

export const styles = StyleSheet.create({
  contentContainer: {
    gap: hp(16),
  },
  columnWrapper: {
    gap: wp(16),
  },
});
